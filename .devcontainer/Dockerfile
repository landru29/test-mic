FROM node:20-slim

WORKDIR /workspace

# Install dependencies for React development
RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*

# Optionally install additional tools (uncomment if needed)
# RUN npm install -g yarn

CMD ["sleep", "infinity"]
